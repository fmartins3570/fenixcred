# 📁 Localização do Arquivo .htaccess

## ✅ O arquivo .htaccess EXISTE nas seguintes localizações:

1. **Raiz do projeto:**
   - Caminho: `/Users/felipemartins/workspace/site-fenix-cred/.htaccess`
   - Tamanho: 6.3 KB
   - Status: ✅ Presente

2. **Pasta dist (para deploy):**
   - Caminho: `/Users/felipemartins/workspace/site-fenix-cred/dist/.htaccess`
   - Tamanho: 6.3 KB
   - Status: ✅ Presente

## 🔍 Como Visualizar o Arquivo

### No Terminal:
```bash
# Ver o arquivo
cat .htaccess

# Ou na pasta dist
cat dist/.htaccess
```

### No VS Code / Cursor:
1. Pressione `Ctrl+Shift+P` (ou `Cmd+Shift+P` no Mac)
2. Digite: `Files: Toggle Excluded Files`
3. O arquivo `.htaccess` aparecerá na lista

### Ou use o arquivo visível:
- `htaccess.txt` - Cópia visível do arquivo (mesmo conteúdo)

## 📤 Para Deploy

O arquivo `.htaccess` já está na pasta `dist/` e será enviado junto com os outros arquivos.

**IMPORTANTE:** No servidor, o arquivo deve se chamar `.htaccess` (com ponto no início), não `htaccess.txt`.
